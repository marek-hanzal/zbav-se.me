import { ToneRoseCls } from "./ToneRoseCls";

/**
 * This is just a gateway CLS instance so it's easier to switch between tones/styles.
 */
export const ThemeCls = ToneRoseCls;

export type ThemeCls = typeof ThemeCls;
