{
	"name": "marek-hanzal/zbav-se.me",
	"description": "Simple but clever c2c marketplace",
	"version": "workspace:*",
	"scripts": {
		"dev": "dotenv -c development -- turbo run dev --parallel",
		"build": "dotenv -c development -- turbo run build --parallel",
		"preview": "dotenv -c development -- turbo run preview --parallel",
		"sdk": "dotenv -c development -- bun x turbo run sdk",
		"translations": "turbo run translations --parallel",
		"format": "biome format --write .",
		"lint": "biome check --write .",
		"knip": "turbo run knip --concurrency=1",
		"typecheck": "turbo run typecheck --parallel",
		"test": "run-s test:vitest test:e2e",
		"test:vitest": "dotenv -c development -- turbo run test --parallel",
		"test:e2e": "bun ./cli/e2e.ts",
		"seed:core": "dotenv -c development -- bun --cwd ./apps/server ./cli/seed-core.ts",
		"seed:interaction": "dotenv -c development -- bun --cwd ./apps/server ./cli/seed-interaction.ts",
		"env:sync": "bun ./cli/env-sync.ts",
		"update": "bun update",
		"sp:fix": "bun x syncpack fix",
		"sp:format": "bun x syncpack format",
		"sp:update": "bun x syncpack update",
		"workflow:format": "run-s sp:format format",
		"workflow:check": "run-s workflow:format lint typecheck",
		"workflow:update": "run-s sp:fix sp:update sp:fix sp:update workflow:format update workflow:check"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.8",
		"@types/node": "25.5.0",
		"dotenv-cli": "11.0.0",
		"npm-run-all2": "8.0.4",
		"syncpack": "14.2.1",
		"turbo": "2.8.20",
		"typescript": "beta"
	},
	"license": "MIT",
	"repository": "marek-hanzal/zbav-se.me.git",
	"overrides": {
		"@types/react": "19.2.14",
		"kysely": "0.28.14"
	},
	"packageManager": "bun@1.3.0",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/**/*"
	]
}
