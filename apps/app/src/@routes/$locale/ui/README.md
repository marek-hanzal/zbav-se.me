# ui

Here are pages using primary navigation at bottom; this is used for the "common" content which does not
need immersive approach.

Route UI shell should delegate rendering to domain page components (for example `@common/nav/page/UiPage.tsx`).
