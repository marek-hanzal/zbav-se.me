{
	"name": "filter.currency",
	"title": "Field: filter.currency",
	"description": "Meaning and invariants of the buyer query filter.currency field.",
	"kind": "field",
	"type": "string",
	"meaning": "Exact single allowed price currency.",
	"actorDependent": false,
	"filterable": true,
	"responseOnly": false,
	"requires": [
		"See zbav://mcp/schema/enum/currency for allowed values."
	],
	"invariants": [
		{
			"label": "Single currency",
			"value": "Use for one exact currency code."
		},
		{
			"label": "Price only",
			"value": "Filters price currency, not priceType or value range."
		}
	],
	"text": "# filter.currency\n\nTL;DR:\n- filter.currency constrains search to one exact currency.\n\nWhat it means:\n- Returned listings must use the selected currency code.\n\nAgent hint:\n- Use this when currency is singular; use currencyIn when multiple currencies are acceptable.\n"
}
