{
	"name": "cursor.size",
	"title": "Field: cursor.size",
	"description": "Meaning and invariants of the buyer query cursor.size field.",
	"kind": "field",
	"type": "number",
	"meaning": "Maximum number of results requested per page.",
	"actorDependent": false,
	"filterable": false,
	"responseOnly": false,
	"invariants": [
		{
			"label": "Page size",
			"value": "Controls result batch size, not ranking or filtering."
		},
		{
			"label": "Bounded",
			"value": "Must remain within the server-supported page-size limits."
		}
	],
	"text": "# cursor.size\n\nTL;DR:\n- cursor.size is the requested page size.\n\nWhat it means:\n- It controls how many results the server should return in one page.\n\nAgent hint:\n- Keep it modest for browse flows unless the workflow explicitly needs larger batches.\n"
}
