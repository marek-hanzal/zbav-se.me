{
	"name": "s3.url",
	"title": "Field: s3.url",
	"description": "Meaning of the short-lived pre-signed PUT URL.",
	"kind": "field",
	"type": "string",
	"meaning": "Short-lived PUT target for the raw binary upload.",
	"actorDependent": false,
	"filterable": false,
	"responseOnly": true,
	"text": "# s3.url\n\nTL;DR:\n- Use s3.url only for the external PUT upload of raw bytes. It is not a stable asset URL and it is not an upload id."
}
