{
	"name": "count.isFilterEmpty",
	"title": "Field: count.isFilterEmpty",
	"description": "Meaning of the filtered-empty flag returned by MCP count tools.",
	"kind": "field",
	"type": "boolean",
	"meaning": "True when the filtered count is zero while some overall total data still exists.",
	"responseOnly": true,
	"text": "# count.isFilterEmpty\n\nTL;DR:\n- count.isFilterEmpty is true when your filter found nothing, but there is still some seller-owned data overall.\n"
}
