{
	"name": "count.where",
	"title": "Field: count.where",
	"description": "Meaning of the internal where count field returned by MCP count tools.",
	"kind": "field",
	"type": "number",
	"meaning": "Count under the app-side where semantics that back the same count calculation.",
	"responseOnly": true,
	"text": "# count.where\n\nTL;DR:\n- count.where exists for parity with backend count semantics.\n- In MCP, prefer count.filter as the public interpretation.\n"
}
