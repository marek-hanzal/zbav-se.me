{
	"name": "s3.extension",
	"title": "Field: s3.extension",
	"description": "Meaning of the requested upload file extension.",
	"kind": "field",
	"type": "string",
	"meaning": "File extension used to build the uploaded object key.",
	"actorDependent": false,
	"filterable": false,
	"responseOnly": false,
	"text": "# s3.extension\n\nTL;DR:\n- s3.extension must come from zbav://mcp/schema/enum/allowed-extension and must match the binary file format."
}
