{
	"name": "filter.id",
	"title": "Field: filter.id",
	"description": "Meaning and invariants of the buyer query filter.id field.",
	"kind": "field",
	"type": "string",
	"meaning": "Exact listing id match for one concrete listing.",
	"actorDependent": false,
	"filterable": true,
	"responseOnly": false,
	"invariants": [
		{
			"label": "Exact",
			"value": "This is the canonical field for exact single-listing lookup by id."
		},
		{
			"label": "Focused",
			"value": "Prefer it when the listing id is already known instead of broad search heuristics."
		}
	],
	"text": "# filter.id\n\nTL;DR:\n- filter.id is the canonical exact id lookup field.\n\nWhat it means:\n- It constrains the query to one concrete listing id.\n\nAgent hint:\n- Use it for precise lookup, especially with buyer.listingFetch.\n"
}
