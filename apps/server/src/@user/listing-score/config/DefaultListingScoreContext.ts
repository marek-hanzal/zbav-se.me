import type { ListingScoreContext } from "../fx/ListingScoreContextFx";

export const DefaultListingScoreContext: ListingScoreContext = {
	listing: 1,
	view: 5,
	favourite: 15,
	//
	ignore: -5,
	flag: -15,
};
