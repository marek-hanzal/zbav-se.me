# @session

Endpoints requiring a session (user); every user can access any data in session
endpoints (privately open).

Session domain may return user-related data (as we've user), but it's considered as public in a protected space (users can access resources in this space freely).
